import { defineConfig } from '@playwright/test';

export default defineConfig({
  // Add your custom Playwright config here if needed
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:8080',
  // },
});